{
  "name": "nautilus-contracts",
  "version": "1.0.0",
  "description": "Nautilus smart contracts (private network edition)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:private": "hardhat run scripts/deploy_private.js --network privatechain"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
